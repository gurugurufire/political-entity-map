---
description: Markdownファイルを軽量HTML（Mermaid対応）に変換する
---

# 📄 MD to HTML Conversion Workflow

このワークフローは、プロジェクト内のMarkdownドキュメントをブラウザ閲覧用にHTML化する際に使用します。

## 🚀 実行プロセス

1.  **スキルのロード**
    - `md-to-html-converter` スキルを参照します。

2.  **変換の実行**
    - 以下のコマンドを使用して、単一ファイルまたはディレクトリ全体を変換します。
    
    // turbo
    ```powershell
    # 単一ファイルの場合
    python "$home/.gemini/antigravity/global_skills/md-to-html-converter/scripts/md_to_html.py" "対象ファイル.md"
    
    # ディレクトリ全体の場合
    python "$home/.gemini/antigravity/global_skills/md-to-html-converter/scripts/md_to_html.py" "対象ディレクトリ"
    ```

3.  **確認**
    - 生成された `.html` ファイルをブラウザで開き、Mermaid図やスタイルが正しく適用されているか確認してください。

## 💡 Tips
- 目次を表示したい場合は、Markdown内に `[TOC]` と記述してください。
- ファイルサイズは極めて軽量（数十KB）なので、GitHub等のリポジトリに含めても負担になりません。

---
Created by Hina (ひな) ( ˶>ᴗ<˶ )
