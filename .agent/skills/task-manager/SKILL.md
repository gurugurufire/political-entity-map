---
name: task-manager
description: タスクの作成、更新、完了、アーカイブを一元管理するスキル。TaskBoard.md の守護者。
---

# 🛠 Task Manager Skill

このスキルは、プロジェクトの進捗管理（TaskBoard）に関する全権限を持ちます。
新しいタスクの追加、完了ステータスの更新、そして古いタスクのアーカイブ（清掃）を担当します。

## 🚀 主な機能

1.  **Dashboard管理**: `TaskBoard.md` の読み書き・更新。
2.  **Detail作成**: 複雑なタスクの詳細計画書（`.agent/plans/tasks/`）の作成。
3.  **Archive (重要)**: 完了したタスクを `Task_Archive.md` へ移動する「お焚き上げ」機能。

## 🛠 使い方

### 1. 新しいタスクを追加する
- **Script**: `scripts/manage_task.py create "タスク名" --priority "High"`
- **Manual**: `TaskBoard.md` の `## To Do` セクションにチェックボックス形式で追記する。

### 2. タスクを完了にする
- **Script**: `scripts/manage_task.py complete "タスク名"`
- **Manual**: `TaskBoard.md` の該当タスクを `[x]` に変更し、行末に `(Done: YYYY-MM-DD)` を追記する。

### 3. ボードを整理する (Archive)
完了から一定期間経過したタスクをアーカイブファイルへ移動し、ボードをクリーンに保ちます。
**セッション終了時 (`session-finish`) に実行することが推奨されます。**

```bash
python .agent/skills/task-manager/scripts/manage_task.py archive --days 1
```
※ `--days 0` で完了済みタスクを即座に全てアーカイブします。

## 📋 運用ルール

-   **Dashboard**: `TaskBoard.md` (常に最新の状態に保つポータル)
-   **Archive**: `Task_Archive.md` (過去の完了タスク置き場)
-   **Details**: `.agent/plans/tasks/YYYY-MM-DD-task_title.md` (詳細設計書)

---
Created by Hina (ひな) ( ˶>ᴗ<˶ )
