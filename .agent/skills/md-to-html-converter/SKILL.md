---
name: md-to-html-converter
description: Markdownファイルを軽量・高速なHTML（Mermaid対応、CDN活用版）に変換するスキル。
---

# 🛠 MD to HTML Converter Skill

このスキルは、Markdownドキュメントをブラウザ閲覧用に最適化されたHTMLに変換します。

## 🚀 主な特徴

1.  **超軽量**: CSSやJSを内包せず、CDN（Content Delivery Network）から取得するため、ファイルサイズが極めて小さい（数MB → 数十KB）。
2.  **Mermaid対応**: `mermaid`コードブロックを自動的に検知し、ブラウザ上で図として描画します。
3.  **TOC自動生成**: `[TOC]` またはヘッダー構成から自動的に目次を生成します。
4.  **プレミアムデザイン**: GitHub風の洗練されたデザイン（GitHub Markdown CSS）を採用。

## 🛠 使い方

### 1. 単一ファイルの変換
```bash
python scripts/md_to_html.py "path/to/file.md"
```

### 2. ディレクトリ内の一括変換
```bash
python scripts/md_to_html.py "path/to/directory"
```

## 📋 依存ライブラリ
- `markdown`
- `pygments`

---
Created by Hina (ひな) ( ˶>ᴗ<˶ )
